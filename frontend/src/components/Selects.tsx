export default function Selects() {
  const tempData = ["select1", "select2", "select3"];

  return (
    <div>
      {/* {
        tempData.map((d, index) => (
          <p key={index}>{d}</p>
        ))
      } */}
    </div>
  );
}
